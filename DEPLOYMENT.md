# 🚀 DEPLOYMENT GUIDE - ELAYA GPT Bot

Полное руководство по развертыванию AI-ассистента на базе знаний Элайи.

---

## 📋 Содержание

1. [Требования к системе](#требования-к-системе)
2. [Предварительная подготовка](#предварительная-подготовка)
3. [Установка зависимостей](#установка-зависимостей)
4. [Конфигурация](#конфигурация)
5. [Загрузка базы знаний](#загрузка-базы-знаний)
6. [Запуск бота](#запуск-бота)
7. [Проверка работоспособности](#проверка-работоспособности)
8. [Troubleshooting](#troubleshooting)
9. [Production режим](#production-режим)

---

## 🖥️ Требования к системе

### Минимальные требования:
- **ОС**: Windows 10/11, Linux, macOS
- **CPU**: 4 ядра (Intel i5 / AMD Ryzen 5 или выше)
- **RAM**: 16 GB (рекомендуется 32 GB)
- **Диск**: 20 GB свободного места (10 GB для моделей + 5 GB для зависимостей)
- **Python**: 3.8 - 3.11 (⚠️ Python 3.12 может иметь проблемы совместимости)

### Рекомендуемые требования:
- **CPU**: 8 ядер
- **RAM**: 16 GB
- **Диск**: SSD с 30+ GB
- **Интернет**: Стабильное подключение (для первоначальной загрузки моделей)

---

## 🔧 Предварительная подготовка

### 1. Установка Python

**Windows:**
```powershell
# Скачайте с python.org
# При установке отметьте "Add Python to PATH"
python --version
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update
sudo apt install python3.10 python3.10-venv python3-pip
python3 --version
```

**macOS:**
```bash
brew install python@3.10
python3 --version
```

### 2. Установка Ollama

**Windows:**
1. Скачайте с [ollama.ai](https://ollama.ai)
2. Запустите установщик
3. Откройте CMD и проверьте:
```cmd
ollama --version
```

**Linux:**
```bash
curl -fsSL https://ollama.ai/install.sh | sh
ollama --version
```

**macOS:**
```bash
brew install ollama
ollama --version
```

### 3. Загрузка моделей Ollama

Откройте терминал и выполните:

```bash
# Запуск Ollama сервера (в отдельном окне)
ollama serve

# В другом окне терминала:
# Модель по умолчанию (7B, ~4.5 GB)
ollama pull qwen2.5:7b-instruct-q4_K_M

# Модель для deep режима (7B, ~4.1 GB)
ollama pull mistral:7b-instruct-q4_K_M
```

⏳ **Время загрузки**: 15-30 минут в зависимости от скорости интернета

### 4. Получение Telegram Bot Token

1. Откройте Telegram
2. Найдите [@BotFather](https://t.me/BotFather)
3. Отправьте `/newbot`
4. Следуйте инструкциям:
   - Имя бота: `ELAYA GPT`
   - Username: `ELAYA_GPT_bot` (или другое доступное)
5. Сохраните токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

---

## 📦 Установка зависимостей

### Автоматическая установка (Windows)

```cmd
# 1. Клонируйте репозиторий
git clone https://github.com/notebookastana/elaya-bot.git
cd elaya-bot

# 2. Запустите автоматический установщик
SETUP.bat
```

Скрипт автоматически:
- ✅ Создаст виртуальное окружение
- ✅ Установит все зависимости
- ✅ Создаст `.env` файл
- ✅ Создаст необходимые папки

### Ручная установка (Linux/macOS)

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/notebookastana/elaya-bot.git
cd elaya-bot

# 2. Создайте виртуальное окружение
python3 -m venv venv

# 3. Активируйте окружение
# Linux/macOS:
source venv/bin/activate
# Windows:
# venv\Scripts\activate

# 4. Обновите pip
pip install --upgrade pip

# 5. Установите зависимости
pip install -r requirements.txt

# 6. Создайте папки
mkdir -p documents data

# 7. Создайте .env файл
cp .env.example .env
```

---

## ⚙️ Конфигурация

### 1. Настройка .env файла

Откройте `.env` в текстовом редакторе:

```bash
# === Обязательные настройки ===
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# === Настройки Ollama ===
OLLAMA_URL=http://localhost:11434/api/generate
DEFAULT_MODEL=qwen2.5:7b-instruct-q4_K_M
DEEP_MODEL=mistral:7b-instruct-q4_K_M

# === Таймауты ===
MAX_STREAM_TIMEOUT=600

# === База данных ===
DB_PATH=elaya_cache.db
```

⚠️ **ВАЖНО**: Замените `TELEGRAM_BOT_TOKEN` на реальный токен от BotFather!

### 2. Настройка bot.py (опционально)

Откройте `bot.py` и настройте под свои нужды:

```python
# === КОНФИГУРАЦИЯ ELAYA ===
BOT_NAME = "ELAYA"
BOT_DESCRIPTION = "AI-ассистент на базе знаний Элайи"
CONTEXT_WINDOW = 10              # Размер контекста диалога
CURRENT_TEMPERATURE = 0.7        # Креативность (0.0-1.0)

# === RAG НАСТРОЙКИ ===
RAG_AUTO_INIT = True             # Автозагрузка при старте
RAG_ALWAYS_SEARCH = True         # Всегда искать в документах
RAG_RELEVANCE_THRESHOLD = 1.5    # Порог релевантности (ниже = строже)
RAG_TOP_K = 5                    # Количество фрагментов для контекста

# === НАСТРОЙКИ ОЧЕРЕДИ ===
MAX_CONCURRENT_REQUESTS = 1      # Одновременных запросов (для CPU)
MAX_QUEUE_SIZE = 10              # Размер очереди
REQUEST_TIMEOUT = 600            # Таймаут запроса (секунды)

# === НАСТРОЙКИ ДЛЯ ГРУПП ===
GROUP_RESPONSE_MODE = "smart"    # "mention_only" | "all" | "smart"
GROUP_CONTEXT_ENABLED = True     # Контекст в группах
```

**Режимы работы в группах:**
- `mention_only` - только на прямые упоминания
- `all` - на все сообщения
- `smart` - умный режим (упоминания + reply + вопросы)

---

## 📚 Загрузка базы знаний

### 1. Подготовка документов

Поместите документы в папку `documents/`:

```
documents/
├── Лекции_Элайи.pdf
├── Теневая_сторона.docx
├── Медитации.txt
└── Практики_осознанности.pdf
```

**Поддерживаемые форматы:**
- ✅ PDF (`.pdf`)
- ✅ Word (`.docx`)
- ✅ Текст (`.txt`)

### 2. Загрузка документов

**Windows:**
```cmd
2_load_documents.bat
```

**Linux/macOS:**
```bash
python load_documents.py
```

⏳ **Процесс загрузки:**
1. Чтение документов
2. Разбиение на фрагменты (chunks)
3. Создание векторных эмбеддингов
4. Сохранение в ChromaDB

**Время обработки**: 2-5 минут на 100 страниц

### 3. Проверка базы знаний

```bash
# В Telegram боте:
/rag_stats
```

Вы увидите:
```
📚 База знаний ELAYA:

📦 Всего фрагментов: 156
📄 Документов: 3

Источники:
   • Лекции_Элайи.pdf: 89
   • Теневая_сторона.docx: 45
   • Медитации.txt: 22
```

---

## 🚀 Запуск бота

### Режим разработки

**Windows:**
```cmd
1_run.bat
```

**Linux/macOS:**
```bash
# Активируйте окружение
source venv/bin/activate

# Запустите бота
python bot.py
```

### Проверка запуска

Вы должны увидеть:
```
🚀 Запуск ELAYA...
✅ Python 3.10.11
✅ Ollama доступна
🔄 Автоинициализация RAG...
✅ RAG активна: 156 чанков из 3 документов
🤖 Bot: @ELAYA_GPT_bot
📚 RAG: ✅ Active
🌡️ Temperature: 0.7
💥 Group mode: smart
```

---

## ✅ Проверка работоспособности

### 1. Базовая проверка

Откройте Telegram и найдите вашего бота:

```
/start
```

Ожидаемый ответ:
```
✨ ELAYA — AI-ассистент на базе знаний Элайи

🌸 Привет, дорогой друг!

Я — твой проводник в мир знаний Элайи...
```

### 2. Проверка RAG

Задайте вопрос из загруженных документов:

```
Что Элайя говорит о любви?
```

Бот должен ответить на основе документов и указать источники.

### 3. Проверка команд

```
/help       # Справка
/stats      # Статистика
/rag_stats  # Статистика базы знаний
/queue      # Очередь запросов
```

### 4. Проверка в группе

1. Добавьте бота в тестовую группу
2. Дайте права на чтение сообщений
3. Проверьте реакцию:

```
@ELAYA_GPT_bot привет!          # ✅ Ответит
Что такое осознанность?         # ✅ Ответит (вопрос)
↩️ (reply на бота)              # ✅ Ответит
@другой_юзер что думаешь?       # ❌ Не ответит
```

---

## 🔧 Troubleshooting

### Проблема: "Ollama недоступна"

**Решение:**
```bash
# Проверьте, запущена ли Ollama
curl http://localhost:11434/api/tags

# Если нет ответа, запустите:
ollama serve
```

### Проблема: "Model not found"

**Решение:**
```bash
# Проверьте установленные модели
ollama list

# Установите недостающие модели
ollama pull qwen2.5:7b-instruct-q4_K_M
ollama pull mistral:7b-instruct-q4_K_M
```

### Проблема: "RAG не инициализируется"

**Решение:**
```bash
# Проверьте зависимости
pip install langchain langchain-community chromadb sentence-transformers

# Проверьте папку documents/
ls documents/

# Попробуйте переинициализировать
# В Telegram:
/rag_clear
# Затем:
python load_documents.py
```

### Проблема: Медленные ответы

**Причины и решения:**

1. **CPU недостаточно мощный:**
   - Используйте модель меньшего размера
   - Уменьшите `RAG_TOP_K` до 3

2. **Слишком много документов:**
   - Уменьшите `RAG_RELEVANCE_THRESHOLD` до 1.0
   - Используйте только ключевые документы

3. **Конкуренция запросов:**
   - Установите `MAX_CONCURRENT_REQUESTS = 2` (если позволяет RAM)

### Проблема: "TELEGRAM_BOT_TOKEN не установлен"

**Решение:**
```bash
# Проверьте .env файл
cat .env

# Убедитесь, что токен указан без пробелов:
TELEGRAM_BOT_TOKEN=1234567890:ABCdef...
```

### Проблема: Бот не отвечает в группе

**Решение:**

1. Проверьте права бота:
   - ✅ Доступ к сообщениям
   - ✅ Возможность отправлять сообщения

2. Проверьте режим:
```python
# В bot.py
GROUP_RESPONSE_MODE = "smart"  # Должно быть "smart" или "all"
```

3. Отключите Privacy Mode:
   - Напишите @BotFather
   - Отправьте `/setprivacy`
   - Выберите бота
   - Выберите `Disable`

---

## 🏭 Production режим

### 1. Использование systemd (Linux)

Создайте файл `/etc/systemd/system/elaya-bot.service`:

```ini
[Unit]
Description=ELAYA GPT Telegram Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/elaya-bot
Environment="PATH=/path/to/elaya-bot/venv/bin"
ExecStart=/path/to/elaya-bot/venv/bin/python bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запуск:
```bash
sudo systemctl daemon-reload
sudo systemctl enable elaya-bot
sudo systemctl start elaya-bot
sudo systemctl status elaya-bot
```

### 2. Использование PM2 (Cross-platform)

```bash
# Установка PM2
npm install -g pm2

# Запуск бота
pm2 start bot.py --name elaya-bot --interpreter python

# Автозапуск при перезагрузке
pm2 startup
pm2 save

# Мониторинг
pm2 logs elaya-bot
pm2 status
```

### 3. Docker (опционально)

Создайте `Dockerfile`:

```dockerfile
FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["python", "bot.py"]
```

Запуск:
```bash
docker build -t elaya-bot .
docker run -d --name elaya-bot \
  -v $(pwd)/documents:/app/documents \
  -v $(pwd)/chroma_db:/app/chroma_db \
  --env-file .env \
  elaya-bot
```

### 4. Мониторинг и логи

**Просмотр логов:**
```bash
tail -f bot.log
```

**Ротация логов** (создайте `logrotate.conf`):
```
/path/to/elaya-bot/bot.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
}
```

### 5. Backup базы знаний

```bash
#!/bin/bash
# backup.sh

DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/backups/elaya-bot"

# Создаём папку
mkdir -p $BACKUP_DIR

# Бэкап ChromaDB
tar -czf $BACKUP_DIR/chroma_db_$DATE.tar.gz chroma_db/

# Бэкап SQLite
cp elaya_cache.db $BACKUP_DIR/elaya_cache_$DATE.db

# Удаляем старые бэкапы (старше 30 дней)
find $BACKUP_DIR -mtime +30 -delete
```

---

## 📊 Мониторинг производительности

### Встроенная статистика

```
/stats       # Общая статистика
/queue       # Состояние очереди
/rag_stats   # Статистика RAG
```

### Метрики для отслеживания:

- **Время ответа**: ~5-30 секунд (зависит от модели)
- **Использование RAM**: 4-8 GB (с загруженной моделью)
- **Использование CPU**: 80-100% во время генерации
- **Размер базы**: ~50-200 MB на 1000 страниц

---

## 🔒 Безопасность

### 1. Защита токенов

```bash
# Убедитесь, что .env в .gitignore
echo ".env" >> .gitignore

# Права доступа (Linux)
chmod 600 .env
```

### 2. Ограничение доступа

В `bot.py` добавьте белый список:

```python
ALLOWED_USERS = [123456789, 987654321]  # ID пользователей

async def is_user_allowed(user_id: int) -> bool:
    return user_id in ALLOWED_USERS
```

### 3. Rate limiting

Уже встроено через систему очередей:
- Максимум 1 запрос одновременно
- Очередь до 10 запросов

---

## 📈 Оптимизация производительности

### 1. Для мощных систем (16+ GB RAM)

```python
# bot.py
MAX_CONCURRENT_REQUESTS = 2  # Или 3
RAG_TOP_K = 7
```

### 2. Для слабых систем (8 GB RAM)

```python
# bot.py
RAG_TOP_K = 3
CONTEXT_WINDOW = 5
```

### 3. Ускорение через кеш

Кеш уже встроен! Повторные запросы отвечают мгновенно.

---

## 🆘 Поддержка

**Автор**: [@bauyrzhan_khamzin](https://t.me/bauyrzhan_khamzin)

**GitHub**: [notebookastana/elaya-bot](https://github.com/notebookastana/elaya-bot)

**Issues**: Создавайте issue на GitHub

**Telegram**: Напишите автору для помощи

---

## ✅ Чеклист развертывания

- [ ] Python 3.8-3.11 установлен
- [ ] Ollama установлена и запущена
- [ ] Модели qwen2.5 и mistral загружены
- [ ] Репозиторий склонирован
- [ ] Виртуальное окружение создано
- [ ] Зависимости установлены
- [ ] .env файл настроен (токен добавлен)
- [ ] Документы помещены в documents/
- [ ] База знаний загружена (2_load_documents.bat)
- [ ] Бот запущен и отвечает
- [ ] Команды протестированы
- [ ] Работа в группе проверена
- [ ] Production режим настроен (опционально)
- [ ] Backup настроен (опционально)

---

**🎉 Поздравляем! ELAYA GPT готова к работе!**

Если возникли вопросы — обращайтесь к автору.
*Автор памятки: Claude + Bauyrzhan Khamzin*
**С любовью, команда ELAYA ✨**

**Удачи с развёртыванием! 🚀**