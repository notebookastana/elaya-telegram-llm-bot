# 🤖 TELEGRAM BOT SETUP - Полное руководство

Пошаговая инструкция по созданию и настройке Telegram бота для ELAYA EVOLUTION.

---

## 📋 Содержание

1. [Создание бота через BotFather](#создание-бота-через-botfather)
2. [Получение и сохранение токена](#получение-и-сохранение-токена)
3. [Настройка команд бота](#настройка-команд-бота)
4. [Настройка описания и аватара](#настройка-описания-и-аватара)
5. [Настройка работы в группах](#настройка-работы-в-группах)
6. [Проверка работоспособности](#проверка-работоспособности)
7. [Дополнительные настройки](#дополнительные-настройки)

---

## 🆕 Создание бота через BotFather

### Шаг 1: Найдите BotFather

1. Откройте Telegram
2. В поиске введите: `@BotFather`
3. Выберите официального бота (с синей галочкой ✓)
4. Нажмите **Start** или напишите `/start`

### Шаг 2: Создайте нового бота

Отправьте команду:
```
/newbot
```

BotFather спросит имя бота:
```
Alright, a new bot. How are we going to call it? 
Please choose a name for your bot.
```

Введите имя (это отображаемое имя, можно любое):
```
ELAYA GPT
```

### Шаг 3: Укажите username

BotFather попросит указать username:
```
Good. Now let's choose a username for your bot. 
It must end in `bot`. Like this, for example: TetrisBot or tetris_bot.
```

Введите username (должен заканчиваться на `bot`):
```
ELAYA_GPT_bot
```

⚠️ **Важно**: Username должен быть уникальным. Если занят, попробуйте:
- `ELAYA_AI_bot`
- `ELAYA_Assistant_bot`
- `Your_ELAYA_bot`

### Шаг 4: Получите токен

BotFather отправит сообщение с токеном:
```
Done! Congratulations on your new bot. You will find it at 
t.me/ELAYA_GPT_bot. You can now add a description...

Use this token to access the HTTP API:
1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567

Keep your token secure and store it safely, it can be used 
by anyone to control your bot.
```

📋 **Скопируйте токен** — он понадобится для конфигурации!

---

## 🔐 Получение и сохранение токена

### Сохранение токена в .env

1. Откройте папку проекта `elaya-evolution`
2. Найдите файл `.env` (если нет — скопируйте `.env.example`)
3. Откройте `.env` в текстовом редакторе (Блокнот, VS Code, etc.)
4. Найдите строку:
   ```
   TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_TELEGRAM_БОТА
   ```
5. Замените на ваш токен:
   ```
   TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz-1234567
   ```
6. Сохраните файл

⚠️ **ВАЖНО**: 
- Не добавляйте пробелы вокруг `=`
- Не берите токен в кавычки
- Не коммитьте `.env` в Git!
- Не делитесь токеном публично!

### Проверка токена

Проверить токен можно через curl:

```bash
curl https://api.telegram.org/bot<YOUR_TOKEN>/getMe
```

Пример:
```bash
curl https://api.telegram.org/bot1234567890:ABCdefGHI/getMe
```

Ожидаемый ответ:
```json
{
  "ok": true,
  "result": {
    "id": 1234567890,
    "is_bot": true,
    "first_name": "ELAYA GPT",
    "username": "ELAYA_GPT_bot",
    "can_join_groups": true,
    "can_read_all_group_messages": false
  }
}
```

---

## 📝 Настройка команд бота

### Установка списка команд

Отправьте BotFather:
```
/mybots
```

Выберите вашего бота → **Edit Bot** → **Edit Commands**

Вставьте следующий список команд:
```
start - Начать общение
help - Справка по всем командам
deep - Глубокий режим размышления
clear - Очистить историю диалога
stats - Статистика использования бота
rag_stats - Статистика базы знаний
queue - Состояние очереди запросов
```

✅ BotFather подтвердит: `Success! Command list updated.`

### Альтернативный способ (через API)

Создайте файл `commands.json`:
```json
{
  "commands": [
    {"command": "start", "description": "Приветствие и информация о боте"},
    {"command": "help", "description": "Справка по всем командам"},
    {"command": "deep", "description": "Глубокий режим анализа"},
    {"command": "clear", "description": "Очистить историю диалога"},
    {"command": "stats", "description": "Статистика использования бота"},
    {"command": "rag_stats", "description": "Статистика базы знаний"},
    {"command": "queue", "description": "Состояние очереди запросов"}
  ]
}
```

Отправьте через curl:
```bash
curl -X POST \
  https://api.telegram.org/bot<YOUR_TOKEN>/setMyCommands \
  -H 'Content-Type: application/json' \
  -d @commands.json
```

---

## 🎨 Настройка описания и аватара

### Описание бота (About) 120 знаков 
можно посмотреть количество знаков на сайте https://involta.ru/tools/length-chars/

Отправьте BotFather:
```
/mybots
```

Выберите бота → **Edit Bot** → **Edit About**

Вставьте описание:
```
✨ ELAYA EVOLUTION — AI-ассистент на базе знаний

💡Я помогу тебе

🌸 Создан с любовью для распространения света и мудрости
```

### Краткое описание (Description) 512 знаков
можно посмотреть количество знаков на сайте https://involta.ru/tools/length-chars/

Выберите **Edit Description**:
```
AI-ассистент ELAYA EVOLUTION на базе знаний Элайи и Бауыржана. Помогаю находить ответы, делюсь духовной мудростью, поддерживаю на пути развития и внутреннего света. Отвечаю на вопросы об учениях, практике, осознанности и гармонии. Создан с любовью для распространения света и добрых знаний. 🌸✨

```

### Аватар бота (Bot Picture)

1. Подготовьте изображение:
   - Формат: JPG или PNG
   - Размер: 512x512 пикселей (рекомендуется)
   - Минимум: 200x200 пикселей
   
2. Отправьте BotFather:
   ```
   /mybots
   ```
   
3. Выберите бота → **Edit Bot** → **Edit Botpic**

4. Отправьте изображение

💡 **Совет**: Используйте изображение, отражающее духовную тематику — мандалы, природа, свет, цветы лотоса и т.п.

---

## 👥 Настройка работы в группах

### Включение Privacy Mode (Отключение)

По умолчанию боты в группах видят только команды и упоминания. Для работы в режиме "smart" нужно отключить Privacy Mode.

1. Отправьте BotFather:
   ```
   /mybots
   ```

2. Выберите бота → **Bot Settings** → **Group Privacy**

3. Выберите **Turn off** (Disable)

✅ BotFather подтвердит: `Success! The new status is: DISABLED`

**Что это даёт:**
- ✅ Бот видит все сообщения в группе
- ✅ Может отвечать на вопросы "в воздух"
- ✅ Работает режим "smart" (reply, вопросы с ?)

### Разрешение добавления в группы

По умолчанию уже включено, но можно проверить:

1. **Bot Settings** → **Allow Groups?**
2. Должно быть: **Turn off** (т.е. разрешение включено)

### Настройка прав администратора (опционально)

Если хотите дать боту админские права:

1. Добавьте бота в группу
2. Откройте настройки группы
3. Администраторы → Добавить администратора
4. Выберите бота
5. Настройте права:
   - ✅ Удаление сообщений (опционально)
   - ✅ Закрепление сообщений (опционально)
   - ❌ Добавление участников (не нужно)
   - ❌ Изменение информации (не нужно)

---

## ✅ Проверка работоспособности

### 1. Тест в личном чате

Найдите бота в Telegram:
```
@ELAYA_GPT_bot
```

Нажмите **Start** и отправьте:
```
/start
```

Ожидаемый ответ:
```
✨ ELAYA — AI-ассистент на базе знаний Элайи

🌸 Привет, дорогой друг!

Я — твой проводник в мир знаний Элайи...
```

Задайте тестовый вопрос:
```
Расскажи о любви
```

Бот должен ответить на основе загруженных документов.

### 2. Тест команд

Попробуйте команды:
```
/help       → Должна показаться справка
/stats      → Статистика
/rag_stats  → Статистика базы знаний
```

### 3. Тест в группе

1. Создайте тестовую группу
2. Добавьте бота
3. Проверьте Privacy Mode (должен быть отключен)
4. Попробуйте разные триггеры:

```
@ELAYA_GPT_bot привет!          # ✅ Должен ответить
```

```
Что такое медитация?            # ✅ Должен ответить (вопрос)
```

```
[Ответьте reply на сообщение бота]
Расскажи подробнее              # ✅ Должен ответить
```

```
Всем привет!                    # ❌ Не должен ответить
```

### 4. Проверка логов

Откройте `bot.log` в папке проекта:

```
2025-01-XX 12:00:00 - elaya - INFO - 🚀 Запуск ELAYA...
2025-01-XX 12:00:01 - elaya - INFO - ✅ Ollama доступна
2025-01-XX 12:00:02 - elaya - INFO - ✅ RAG активна: 156 чанков
2025-01-XX 12:00:03 - elaya - INFO - 🤖 Bot: @ELAYA_GPT_bot
```

---

## ⚙️ Дополнительные настройки

### Inline Mode (опционально)

Если хотите, чтобы бот работал в inline режиме (вызов в любом чате):

1. Отправьте BotFather:
   ```
   /mybots
   ```

2. Выберите бота → **Bot Settings** → **Inline Mode**

3. Выберите **Turn on**

4. Установите placeholder:
   ```
   Задайте вопрос Элайе...
   ```

### Webhook вместо polling (для production)

По умолчанию бот использует polling (постоянные запросы к API). Для production рекомендуется webhook.

**Требования:**
- Сервер с HTTPS
- Статический IP или домен
- SSL сертификат

**Установка webhook:**

```python
# Добавьте в bot.py (для production)
import ssl

WEBHOOK_HOST = "https://your-domain.com"
WEBHOOK_PATH = f"/bot/{TELEGRAM_BOT_TOKEN}"
WEBHOOK_URL = f"{WEBHOOK_HOST}{WEBHOOK_PATH}"

# SSL контекст
WEBHOOK_SSL_CERT = "/path/to/cert.pem"
WEBHOOK_SSL_PRIV = "/path/to/private.key"

# Установка webhook
await bot.set_webhook(
    url=WEBHOOK_URL,
    certificate=open(WEBHOOK_SSL_CERT, 'rb')
)
```

### Payments API (опционально)

Если планируете монетизацию:

1. BotFather → **Bot Settings** → **Payments**
2. Выберите платёжного провайдера
3. Следуйте инструкциям провайдера

### Bot Stats через BotFather

Проверить статистику использования:

```
/mybots
```

Выберите бота → **Bot Stats**

Показывает:
- Количество пользователей
- Количество сообщений
- Статистику по дням

---

## 🔒 Безопасность

### Защита токена

✅ **DO:**
- Храните токен в `.env` файле
- Добавьте `.env` в `.gitignore`
- Используйте переменные окружения на сервере
- Регулярно обновляйте токен (если был скомпрометирован)

❌ **DON'T:**
- Не коммитьте токен в Git
- Не публикуйте токен в Issues/Forums
- Не храните токен в открытом виде в коде
- Не делитесь токеном с неизвестными

### Сброс токена

Если токен был скомпрометирован:

1. BotFather → `/mybots`
2. Выберите бота → **API Token**
3. **Revoke current token**
4. Получите новый токен
5. Обновите `.env` файл
6. Перезапустите бота

### Ограничение доступа

В `bot.py` добавьте белый список пользователей:

```python
# В начале файла
ALLOWED_USERS = [123456789, 987654321]  # Telegram User IDs

# В обработчике сообщений
@dp.message()
async def handle_message(message: types.Message):
    if message.from_user.id not in ALLOWED_USERS:
        await message.reply("⛔ Доступ запрещён")
        return
    # Остальная логика...
```

---

## 📊 Мониторинг и аналитика

### Встроенная статистика

Используйте команды:
```
/stats      # Статистика использования
/queue      # Очередь запросов
/rag_stats  # Статистика базы знаний
```

### Внешняя аналитика

Рекомендуемые сервисы:
- **Botan.io** — аналитика для ботов
- **Amplitude** — продвинутая аналитика
- **Mixpanel** — отслеживание событий

### Логирование

Логи хранятся в `bot.log`. Для продвинутого логирования:

```python
import logging
from logging.handlers import RotatingFileHandler

handler = RotatingFileHandler(
    'bot.log',
    maxBytes=10*1024*1024,  # 10 MB
    backupCount=5,
    encoding='utf-8'
)

logging.basicConfig(
    handlers=[handler, logging.StreamHandler()],
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)
```

---

## 🆘 Troubleshooting

### Бот не отвечает

**Проблема**: Бот создан, но не отвечает на сообщения.

**Решения**:
1. Проверьте, запущен ли бот (`python bot.py`)
2. Проверьте токен в `.env`
3. Проверьте логи (`bot.log`)
4. Проверьте, работает ли Ollama (`ollama serve`)

### Privacy Mode не отключается

**Проблема**: Бот в группе не видит сообщения.

**Решения**:
1. BotFather → `/setprivacy` → Выберите бота → Disable
2. Удалите бота из группы
3. Перезапустите бота
4. Добавьте бота обратно

### Команды не работают

**Проблема**: Команды не показываются или не работают.

**Решения**:
1. Проверьте настройку команд в BotFather
2. Убедитесь, что команды правильно написаны (без `/` в BotFather)
3. Перезапустите приложение Telegram
4. Попробуйте удалить и начать диалог заново

### Бот не добавляется в группу

**Проблема**: Не получается добавить бота в группу.

**Решения**:
1. Проверьте: Bot Settings → Allow Groups → должно быть включено
2. Проверьте, не забанен ли бот Telegram
3. Проверьте настройки приватности группы

---

## 📚 Дополнительные ресурсы

### Официальная документация

- **Telegram Bot API**: https://core.telegram.org/bots/api
- **BotFather Commands**: https://core.telegram.org/bots#botfather
- **aiogram Documentation**: https://docs.aiogram.dev/

### Примеры и туториалы

- **aiogram Examples**: https://github.com/aiogram/aiogram/tree/dev-3.x/examples
- **Telegram Bot Samples**: https://core.telegram.org/bots/samples

### Сообщество

- **Telegram: @aiogram_live** — aiogram community
- **GitHub Issues**: github.com/notebookastana/elaya-bot/issues
- **Автор**: @bauyrzhan_khamzin

---

## ✅ Чеклист настройки Telegram бота

- [ ] Создан бот через @BotFather
- [ ] Получен и сохранён токен в `.env`
- [ ] Настроены команды бота
- [ ] Установлено описание (About, Description)
- [ ] Загружен аватар бота
- [ ] Отключен Privacy Mode (для групп)
- [ ] Включено Allow Groups
- [ ] Проверена работа в личном чате
- [ ] Проверена работа в группе
- [ ] Проверены все команды
- [ ] Настроена безопасность токена
- [ ] Настроено логирование

---

**🎉 Поздравляем! Ваш Telegram бот полностью настроен!**

Теперь можно использовать @ELAYA_GPT_bot для распространения мудрости Элайи.
*Автор памятки: Claude + Bauyrzhan Khamzin*
**С любовью, команда ELAYA ✨**
